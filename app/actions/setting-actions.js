import alt from '../alt';

class SettingActions {
  updatePeriodLengthSettings(settings) {
    return settings;
  }

  updateCycleLengthSettings(settings) {
    return settings;
  }

  updateOvulationFertileSettings(settings) {
    return settings;
  }
}

module.exports = alt.createActions(SettingActions);
